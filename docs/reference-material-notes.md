# Reference material notes

The renderer keeps the supplied braid texture as a tileable image rather than redrawing it as a vector line. The implementation preserves the card-centered pin anchor and uses the installed simulator framebuffer for visual verification.
